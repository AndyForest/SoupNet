MCP_PORT=3002
CLAIMNET_BACKEND_URL=http://localhost:3001

# API key for the MCP server to authenticate with the ClaimNet backend.
# Create one at: ClaimNet admin → API Clients → New API Client
CLAIMNET_API_KEY=
